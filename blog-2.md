Title : How do Generics allow you to build reusable components and functions that stay strictly typed regardless of the data structures passed in?
Introduction :
প্রোগ্রামিং শেখার শুরুতে আমরা সবাই একটা কমন সমস্যায় পড়ি—কোড  (Code Reusability)। আমরা চাই এমন একটা ফাংশন বা কম্পোনেন্ট বানাতে যা বিভিন্ন ধরণের ডাটা স্ট্রাকচার নিয়ে কাজ করতে পারবে। কিন্তু সমস্যা হলো, ডাটা টাইপ ডাইনামিক করতে গিয়ে যদি আমরা any বা লুজ টাইপিং ব্যবহার করি, তাহলে কম্পাইল-টাইম টাইপ চেকিংয়ের (Strict Typing) ক্ষমতা হারিয়ে যায়। এখানেই জেনেরিক্স (Generics) খুব ভাল কাজ করে ।



Body Paragraphs: 
সাধারণত কোনো ফাংশনে আমরা ভ্যালু বা ডাটা পাস করি আর্গুমেন্ট হিসেবে। আর জেনেরিক্সের ম্যাজিক হলো, এটি আমাদের ভ্যালুর পাশাপাশি  (Type)-কেও argument  হিসেবে পাস করার সুযোগ দেয়।

ধরা যাক, আমরা একটা identity ফাংশন বানাতে চাই, যা ইনপুট হিসেবে যা পাবে, আউটপুট হিসেবে ঠিক সেটাই রিটার্ন করবে।

জেনেরিক্স ছাড়া সমস্যা (The Problem)
আমরা যদি জেনেরিক্স ছাড়া শুধু নির্দিষ্ট টাইপ দিয়ে এটি করতে যাই, তবে প্রতিটা টাইপের জন্য আলাদা ফাংশন লিখতে হবে:

TypeScript
function logNumber(arg: number): number {
    return arg;
}

function logString(arg: string): string {
    return arg;
}
এটা মোটেও ভালো কোনো সমাধান নয়। 

TypeScript
function logAnything(arg: any): any {
    return arg;
}
এখানেই বড় সমস্যা।  any ব্যবহার করলে ফাংশনটি যেকোনো ডাটা নিতে পারবে ঠিকই, কিন্তু সেটার Strict Typing নষ্ট হয়ে যাবে। আমি  যদি ইনপুটে number দেই , কম্পাইলার আর ট্র্যাক রাখতে পারবে না যে আউটপুটটাও একটা number-ই আসবে।

জেনেরিক্সের মাধ্যমে সমাধান (The Solution)
জেনেরিক্স আমাদের এই সমস্যার সমাধান দেয় Type Variable (সাধারণত <T> দিয়ে প্রকাশ করা হয়) ব্যবহারের মাধ্যমে। এটি পাস করা ডাটার টাইপটিকে নিজে থেকেই ক্যাপচার বা লক করে ফেলে।

TypeScript
 এখানে <T> হলো টাইপ ভেরিয়েবল
function logWithGeneric<T>(arg: T): T {
    return arg;
}

ব্যবহার করার নিয়ম:
let output1 = logWithGeneric<string>("হ্যালো ওয়ার্ল্ড"); (এখানে T হয়ে গেল string) 
let output2 = logWithGeneric<number>(100);           (এখানে T হয়ে গেল number) 
ডাটা স্ট্রাকচার যাই হোক, টাইপ থাকবে স্ট্রিক্ট

Conclusion :
নতুন প্রোগ্রামার হিসেবে আমার কাছে জেনেরিক্সকে একটি "টাইপ ব্লুপ্রিন্ট" এর মতো মনে হয়। এটি কোড ডুপ্লিকেশন কমায় এবং any ব্যবহার করার ঝুঁকি থেকে আমাদের বাঁচায়। সংক্ষেপে বলতে গেলে, জেনেরিক্স ব্যবহারের ফলেই আমরা এমন ফ্লেক্সিবল ও রিইউজেবল ফাংশন এবং কম্পোনেন্ট তৈরি করতে পারি, যা ডাটা স্ট্রাকচারের ভিন্নতা সত্ত্বেও কোডের স্ট্রিক্ট টাইপিং (Strict Typing) এবং সুরক্ষাকে বিন্দুমাত্র নষ্ট হতে দেয় না।